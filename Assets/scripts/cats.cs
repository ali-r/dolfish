using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cats : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("hello");
        Debug.Log((this.cat1).ToString());
        

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
